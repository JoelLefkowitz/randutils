[tox]
envlist = 
    py36
    py37
    py38

[testenv]
deps = 
    .[tests]
    pytest
    pytest-cov
    
commands = 
    coverage run --include randutils/* -m    \
    pytest --doctest-modules randutils    

    coverage run --include randutils/* -a -m \
    pytest

    coverage report
    coverage xml
